[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "npm ci",
  "cd server && npm ci",
  "cd ../client && npm ci",
  "cd .."
]

[phases.build]
cmds = [
  "npm run build:server",
  "npm run build:client"
]

[start]
cmd = "npm run start:server"